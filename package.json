{
  "name": "inventory-intelligence",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"corepack pnpm --filter web dev\" \"corepack pnpm --filter api dev\" \"corepack pnpm --filter ml dev\"",
    "dev:web": "corepack pnpm --filter web dev",
    "dev:api": "corepack pnpm --filter api dev",
    "dev:ml": "corepack pnpm --filter ml dev",
    "build": "corepack pnpm --filter web build && corepack pnpm --filter api build && corepack pnpm --filter ml build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "db:migrate": "corepack pnpm --filter api db:migrate",
    "db:seed": "corepack pnpm --filter api db:seed",
    "ml:train": "corepack pnpm --filter ml train",
    "test": "corepack pnpm --filter api test && corepack pnpm --filter web test",
    "lint": "corepack pnpm -r lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
